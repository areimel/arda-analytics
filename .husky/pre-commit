#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Temporarily disabled for Phase 3 completion
# pnpm --filter @arda-analytics/plugin exec lint-staged
echo "Pre-commit hook temporarily disabled - Phase 3 setup" 